#include iostream
#include vector

using namespace std;

int main() {

int n;
cin  n;

int arr[n];
vector int vec= {};

 for (int i=0; in; i++){
    cinarr[i];
 }

 int counter = 0;

   for (int i=0; in; i++){

    if (arr[i]!=0){
        vec.push_back(arr[i]);
    }
   }




  for (int i=0; in; i++){

    if (arr[i]==0 && arr[n-1]!=0){
        swap(arr[i],arr[n-1]);
    }

    else if (arr[i]==0 && arr[n-1]==0){

        for (int j=n-2; ji; j--){

            if (arr[i]==0 && arr[j]!= 0){
                swap(arr[i],arr[j]);
                 break;
            }
        }

    }

  }

  for (int i=0; ivec.size(); i++){
    cout  vec[i] ;
  }

  for (int i=0; in; i++){

      if (arr[i]==0 && i!=n-1){
        cout  arr[i] ;
      }

      else if (arr[i]==0 && i==n-1){
        cout arr[i];
      }

    }



    return 0;
}
